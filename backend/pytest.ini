[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    slow: slow integration tests (excluded by default; run with -m slow)
    integration: tests requiring a running PostgreSQL test database
addopts = -p no:cacheprovider -q
filterwarnings =
    ignore::DeprecationWarning
